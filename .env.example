# Convex (get from `npx convex dev`)
EXPO_PUBLIC_CONVEX_URL=https://your-project.convex.cloud

# Clerk (optional — for auth sync with WinFlowz)
EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY=pk_test_...

# API keys are stored securely on-device via expo-secure-store
# Set them in the Settings tab of the app
