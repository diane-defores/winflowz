---
artifact: bug_record
metadata_schema_version: "1.0"
artifact_version: "0.1.0"
project: "voiceflowz"
created: "2026-05-11"
updated: "2026-05-11"
status: fix-attempted
source_skill: "sf-fix"
scope: "bug"
owner: "codex"
bug_id: "BUG-2026-05-11-001"
title: "Android overlay does not appear or react from Settings"
bug_status: "fix-attempted"
severity: "high"
reported_by: "user"
first_observed: "2026-05-11"
last_observed: "2026-05-11"
environment: "Android real device, debug APK sha=7d6fd2d run=25662735886"
reproducibility: "reported"
redaction_status: "redacted"
related_bugs: []
related_artifacts:
  - "specs/android-overlay-flutter-parity-repair.md"
  - "specs/android-ime-voiceflowz-keyboard.md"
confidence: high
risk_level: high
security_impact: no
docs_impact: yes
evidence:
  - "User reported on 2026-05-11 that the overlay does not appear and the overlay button does not trigger anything despite all permissions being allowed."
  - "User-provided diagnostic: overlay_status enabled=false; requested=false; running=false; overlay_permission=true; accessibility_permission=true; delivery=injectionAndClipboard; size=1.0; opacity=0.800000011920929; busy=false."
depends_on: []
supersedes: []
next_step: "/sf-ship BUG-2026-05-11-001"
---

# Bug Record: BUG-2026-05-11-001

## Summary

- Title: Android overlay does not appear or react from Settings
- Current status: `fix-attempted`
- Severity: `high`
- Owner: codex
- Next command: `/sf-ship BUG-2026-05-11-001`

## Reproduction

- Preconditions:
  - Android real device.
  - Installed debug APK reports `build: sha=7d6fd2d | run=25662735886 | ref=master`.
  - Overlay permission is granted.
  - Accessibility permission is granted.
- Steps:
  1. Open VoiceFlowz.
  2. Go to Settings.
  3. Use the overlay enable/start control.
  4. Observe whether the native floating overlay appears or any Settings status changes.

## Expected Behavior

- The Settings overlay control should call the native overlay bridge.
- With overlay permission granted, `requestedEnabled` should become true and the foreground overlay service should start.
- A native floating overlay bubble should appear.
- If Android blocks the foreground service or `WindowManager.addView`, Settings diagnostics should show a concise native failure code.

## Observed Behavior

- The overlay does not appear.
- The user reports that the overlay button does not trigger anything.
- Diagnostic still reports `enabled=false`, `requested=false`, and `running=false` even though `overlay_permission=true` and `accessibility_permission=true`.
- Existing `recent_events` contains tab and screen-build activity, but no overlay action event, making the current logs insufficient to identify whether the failure is UI tap handling, Dart bridge invocation, native preference persistence, service startup, or overlay view attachment.

## Evidence

- User report in chat on 2026-05-11.
- Redacted diagnostic excerpt copied by user:
  - `diagnostic_version: 5`
  - `generated_at_utc: 2026-05-11T10:23:41.387070Z`
  - `build: sha=7d6fd2d | run=25662735886 | ref=master`
  - `platform_capabilities: android=true; overlay=true; keyboard_ime=true`
  - `overlay_status: enabled=false; requested=false; running=false; overlay_permission=true; accessibility_permission=true; delivery=injectionAndClipboard; size=1.0; opacity=0.800000011920929; busy=false`

## Diagnosis Notes

| Date UTC | Skill | Author | Hypothesis | Findings | Outcome |
|----------|-------|--------|------------|----------|---------|
| 2026-05-11 10:27:05 UTC | sf-test | codex | The current diagnostic cannot distinguish a missed Flutter tap from a native overlay service/view failure. | User diagnostic has permissions granted but `requested=false` and no overlay-related recent event; Settings only logged screen builds before this fix. | supported |
| 2026-05-11 10:42:45 UTC | sf-fix | codex | Overlay activation may be blocked by treating the visible bubble service as a `microphone` foreground service and by requiring pre-enabled state before Start. | Android official docs say microphone foreground services require `RECORD_AUDIO` runtime permission and are subject to while-in-use restrictions, while `specialUse` has no runtime prerequisite for valid foreground service cases not covered by other types. Current overlay start is for a user-controlled bubble, not immediate microphone capture. | supported |

## Fix Attempts

| Date UTC | Skill | Author | Changes | Validation | Result | Next Step |
|----------|-------|--------|---------|------------|--------|-----------|
| 2026-05-11 10:27:05 UTC | sf-test | codex | Added compact overlay diagnostics: Flutter action breadcrumbs for status/toggle/start/stop/cancel/appearance, native bridge call/error events, native service lifecycle/view events, status fields for service state, event queue size, and last native event. | `flutter analyze`; `flutter test test/widget_test.dart`; `flutter test`; `git diff --check`; Android Kotlin compile attempted locally but blocked by missing Android SDK/local.properties. | partial | Ship/install a new APK from CI and retest overlay activation. |
| 2026-05-11 10:42:45 UTC | sf-fix | codex | Changed `OverlayForegroundService` from `microphone` foreground service type to `specialUse` with manifest subtype; changed Android 14 `startForeground` type to `FOREGROUND_SERVICE_TYPE_SPECIAL_USE`; made native `startOverlayRecording` persist overlay enabled before start instead of failing with `OVERLAY_DISABLED`; clarified Settings overlay status/copy; documented overlay service type. | `flutter analyze`; `flutter test test/widget_test.dart`; `flutter test`; `git diff --check`; `./gradlew :app:compileDebugKotlin` blocked locally by missing Android SDK/local.properties. | partial | `/sf-ship BUG-2026-05-11-001`, then install APK and `/sf-test --retest BUG-2026-05-11-001`. |

## Retest History

| Date UTC | Skill | Retest Command | Environment | Result | Notes |
|----------|-------|----------------|-------------|--------|-------|
| 2026-05-11 10:23:41 UTC | sf-test | Manual Android real-device overlay + IME QA | Android debug APK sha=7d6fd2d run=25662735886 | failed | Overlay did not appear; button appeared to do nothing despite permissions granted. |
| 2026-05-11 10:27:05 UTC | sf-test | `flutter analyze`, `flutter test`, `git diff --check` | local Linux ARM64 | passed | Dart/static checks passed after diagnostic additions; native Android compile still needs SDK/CI proof. |
| 2026-05-11 10:27:05 UTC | sf-test | `./gradlew :app:compileDebugKotlin` | local Linux ARM64 | blocked | Gradle configured build logic, then failed because Android SDK location is not configured in `android/local.properties` or `ANDROID_HOME`. |
| 2026-05-11 10:42:45 UTC | sf-fix | `flutter analyze`; `flutter test test/widget_test.dart`; `flutter test`; `git diff --check` | local Linux ARM64 | passed | Dart/static checks passed after foreground-service type and Settings flow changes. |
| 2026-05-11 10:42:45 UTC | sf-fix | `./gradlew :app:compileDebugKotlin` | local Linux ARM64 | blocked | Android SDK location is still not configured in `android/local.properties` or `ANDROID_HOME`; native compile proof must come from CI/Blacksmith. |

## Related Bugs / Artifacts

- Related bugs:
  - none
- Related artifacts:
  - `TEST_LOG.md`
  - `lib/features/settings/presentation/settings_screen.dart`
  - `lib/core/platform/android_overlay_bridge.dart`
  - `android/app/src/main/kotlin/com/voiceflowz/voiceflowz/MainActivity.kt`
  - `android/app/src/main/kotlin/com/voiceflowz/voiceflowz/OverlayForegroundService.kt`
  - `android/app/src/main/kotlin/com/voiceflowz/voiceflowz/OverlayEventQueue.kt`
  - `android/app/src/main/AndroidManifest.xml`
  - `docs/OVERLAY_ANDROID.md`

## Lifecycle Notes

Canonical bug lifecycle statuses:

- `open`
- `needs-info`
- `needs-repro`
- `in-diagnosis`
- `fix-attempted`
- `fixed-pending-verify`
- `closed`
- `closed-without-retest`
- `duplicate`
- `wontfix`

Status transitions must stay explicit in this dossier.

## Redaction Status

- Current redaction status: `redacted`
- Redaction notes:
  - User diagnostic stated `secret_values_redacted: true`.
  - No raw secrets, tokens, cookies, personal payloads, screenshots, or headers were persisted.

## Closure Criteria

- A new CI-built installed APK shows overlay action breadcrumbs in copied diagnostics after tapping the overlay controls.
- With permissions granted, overlay activation changes `requestedEnabled` to true and either shows the bubble or reports a native failure code such as foreground start or view add failure.
- Manual Android retest confirms the overlay appears and can be started/stopped/canceled, or a separate fix is opened from the new diagnostic evidence.

## Next Step

`/sf-ship BUG-2026-05-11-001`, then `/sf-test --retest BUG-2026-05-11-001`
